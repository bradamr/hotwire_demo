<div class="comment">
  <div class="title">
    <%= kitten_emojis.sample %>&nbsp;<%= comment.title %>
  </div>
  <div class="body">
    <%= comment.body %>
  </div>

  <!-- Change area -->
  <%= render partial: 'likes/like', locals: { comment: comment } %>
</div>